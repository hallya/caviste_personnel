export const FORM_CONFIG = {
  MESSAGE_TEXTAREA_ROWS: 4,
} as const;

export const API_ENDPOINTS = {
  FORMATIONS_REGISTER: "/api/formations/register",
} as const;

export const NOTIFICATION_CONFIG = {
  LOADING_ID: "formation-submission",
  AUTO_CLOSE_DELAY: 5000,
  ERROR_AUTO_CLOSE_DELAY: 8000,
} as const;