// Re-export commonly used constants with shorter names for convenience
export {
  API_TEST_CONFIG as API,
  TEST_URLS as URLS,
  ERROR_MESSAGES as ERRORS,
} from './api';

export {
  SHOPIFY_TEST_IDS as IDS,
  SHOPIFY_HANDLES as HANDLES,
  SHOPIFY_DATA as DATA,
} from './shopify';