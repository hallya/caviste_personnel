#import CartDetailed from "../fragments/cart-detailed.graphql"

mutation CartCreate($lines: [CartLineInput!]!) {
  cartCreate(input: { lines: $lines }) {
    cart {
      ...CartDetailed
    }
    userErrors {
      field
      message
    }
  }
}
